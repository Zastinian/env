{
  "name": "@hedystia/env",
  "version": "1.0.2",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "commonjs",
  "scripts": {
    "publish": "bun run types && bun run build && npm publish",
    "build": "bun build --target=node --format=cjs src/index.ts --outfile dist/index.js --minify",
    "types": "bunx tsc --noEmit false --emitDeclarationOnly"
  },
  "author": "contact@hedystia.com",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Zastinian/env.git"
  },
  "bugs": {
    "url": "https://github.com/Zastinian/env/issues"
  },
  "keywords": ["env", "npm", "validation"],
  "devDependencies": {
    "@biomejs/biome": "2.3.13",
    "@types/bun": "1.3.8"
  },
  "engines": {
    "node": ">=20.6.0",
    "bun": ">=1.2.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
